/** Re-export citywide corridor filter (kept for older import paths). */
export {
  filterCorridorOffPlanCandidates,
  filterEastOffPlanCandidates,
  isCorridorOffPlanCandidate,
  isEastOffPlanCandidate,
} from "@/lib/imports/corridor-offplan-filter";
